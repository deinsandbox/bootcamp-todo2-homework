import { SizeUpDirective } from './size-up.directive';

describe('SizeUpDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeUpDirective();
    expect(directive).toBeTruthy();
  });
});
